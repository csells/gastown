# ==============================================================================
# Gas Town Dashboard Configuration
# ==============================================================================
# Copy this file to .env and customize for your environment
# All variables are optional unless marked as REQUIRED
# ==============================================================================

# ------------------------------------------------------------------------------
# SERVER CONFIGURATION
# ------------------------------------------------------------------------------

# PORT - HTTP server port number
#
# The port on which the dashboard web server will listen.
# Must be between 1-65535. Common choices are 8080, 3000, or 8000.
# Make sure this port is not already in use by another application.
#
# Default: 8080
# Example: PORT=3000
PORT=8080

# LOG_LEVEL - Application logging verbosity
#
# Controls how much logging output the dashboard produces.
# Valid values: debug, info, warn, error
#   - debug: Verbose output including CLI commands executed (useful for development)
#   - info:  General information about dashboard operations (recommended for production)
#   - warn:  Only warnings and errors
#   - error: Only error messages
#
# Default: info
# Example: LOG_LEVEL=debug
LOG_LEVEL=info

# ------------------------------------------------------------------------------
# BEADS CONFIGURATION
# ------------------------------------------------------------------------------

# BEADS_DIR - Path to the Beads database directory
#
# Location of the .beads directory containing beads.db SQLite database.
# This is where the dashboard will look for convoy and issue data.
#
# Can be:
#   - Relative path: .beads (looks in current directory)
#   - Absolute path: /home/user/gt/mayor/.beads
#   - Environment-based: ${HOME}/gt/mayor/.beads
#
# The directory must contain a beads.db file with the issues and dependencies tables.
# If not set, defaults to .beads in the current working directory.
#
# Default: .beads
# Example: BEADS_DIR=/home/user/gt/mayor/.beads
# Example: BEADS_DIR=../.beads
BEADS_DIR=.beads

# ------------------------------------------------------------------------------
# GITHUB CONFIGURATION
# ------------------------------------------------------------------------------

# GITHUB_REPOS - GitHub repositories to monitor for pull requests
#
# Comma-separated list of GitHub repositories to check for open PRs.
# Format: owner/repo,owner/repo,...
#
# The dashboard will query these repos using the GitHub CLI (gh) to display:
#   - Open pull requests
#   - CI status (pass/fail/pending)
#   - Merge readiness (ready/conflict/pending)
#
# Requirements:
#   - GitHub CLI (gh) must be installed and authenticated (run: gh auth login)
#   - You must have read access to the repositories
#
# If left empty, the merge queue section will show "No GitHub repos configured"
#
# Default: (empty - no repos monitored)
# Example: GITHUB_REPOS=octocat/Hello-World,microsoft/vscode
# Example: GITHUB_REPOS=yourusername/yourrepo
GITHUB_REPOS=

# ------------------------------------------------------------------------------
# OPTIONAL CONFIGURATION
# ------------------------------------------------------------------------------

# GT_TOWN_ROOT - Gas Town workspace root directory
#
# Path to the Gas Town "town" root directory (typically contains town.json).
# Used as a fallback for workspace detection when the dashboard needs to
# locate the Gas Town workspace.
#
# This is optional - the dashboard will work without it by using the current
# working directory and BEADS_DIR configuration.
#
# Only set this if:
#   - You're running the dashboard from outside the Gas Town workspace
#   - You need explicit workspace path configuration
#   - Polecat sessions set this variable and you want consistency
#
# Default: (empty - not set)
# Example: GT_TOWN_ROOT=/home/user/gt/mayor
# Example: GT_TOWN_ROOT=${HOME}/workspace/gastown
GT_TOWN_ROOT=

# ==============================================================================
# QUICK START EXAMPLES
# ==============================================================================
#
# Minimal configuration (local development):
#   PORT=8080
#   LOG_LEVEL=info
#   BEADS_DIR=.beads
#   GITHUB_REPOS=
#
# Full configuration example:
#   PORT=8080
#   LOG_LEVEL=debug
#   BEADS_DIR=/home/user/gt/mayor/.beads
#   GITHUB_REPOS=yourorg/repo1,yourorg/repo2
#   GT_TOWN_ROOT=/home/user/gt/mayor
#
# ==============================================================================
# VERIFICATION
# ==============================================================================
# After configuration, verify your setup:
#
# 1. Check CLI tools are installed:
#    bd --version
#    tmux -V
#    gh --version
#    sqlite3 --version
#
# 2. Check GitHub authentication:
#    gh auth status
#
# 3. Check beads database exists:
#    ls -la ${BEADS_DIR}/beads.db
#
# 4. Start the dashboard:
#    npm run dev
#
# ==============================================================================
